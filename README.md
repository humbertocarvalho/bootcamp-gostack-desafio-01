# Desafio 01. Conceitos do NodeJS

Aplicação utilizando Express para armazenamento de projetos e suas tarefas.

## Rotas

- `POST /projects`

- `GET /projects`

- `PUT /projects/:id`

- `DELETE /projects/:id`

- `POST /projects/:id/tasks`
